# Browser-safe values. These are exposed to the KUT client at build time.
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=

# Server/CI only. Never prefix this with PUBLIC_, commit it, or send it to a
# browser.
SUPABASE_SERVICE_ROLE_KEY=
