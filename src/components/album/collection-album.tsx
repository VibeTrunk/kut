import type { ReactNode } from "react";
import Link from "next/link";
import { AlbumKeyboardNavigation } from "@/components/album/album-keyboard-navigation";
import { LensMenu } from "@/components/album/lens-menu";
import { LiveCard, type LiveCardPlayer } from "@/components/live-card";
import { CollectionHeader, Completion } from "@/components/album/collection-header";
import { ARCHETYPES, ARCHETYPE_LABELS } from "@/game/archetypes";
import type { RarityTier } from "@/game/rating-engine";
import { applyLens, buildSlots, lensParam, paginate, parseLens, spreadFor, spreadPartner, type AlbumCollectionCard, type AlbumDirectoryPlayer, type AlbumSlot } from "@/lib/album";

const TIERS: RarityTier[] = ["common", "bronze", "silver", "gold", "holo", "elite"];
type Props = { roster: AlbumDirectoryPlayer[]; cards: AlbumCollectionCard[]; lensValue?: string; pageValue?: string; ownPlayerId: string | null; photoUrls: Map<string, string>; clubValue: number | null };
function href(lens: string, page: string | number) { const p = new URLSearchParams(); if (lens !== "all") p.set("lens", lens); p.set("page", String(page)); return `/club/collection?${p}`; }

/* The box every slot shares: a 1rem number strip above a 5:7 frame. Collected and
   empty slots MUST render the same box. When they differed — the collected slot
   carried the strip, the empty slot placed its number inside the frame — the two
   states were ~1rem apart in height, and because each leaf is its own grid in its
   own <article>, row heights are computed per page: any row where one leaf held a
   card and the other held a gap pushed the facing leaves out of horizontal
   alignment for the rest of the spread (KB-003). Every overlay a slot renders
   positions against this wrapper, so `top-5` / `top-8` / `bottom-1` below are
   measured from the top of the number strip, not the top of the card. */
function SlotFrame({ index, children }: { index: number; children: ReactNode }) {
  return <div className="relative pt-4"><span className="absolute left-0 top-0 text-[0.65rem] font-black tabular-nums text-ink-faint">{index}</span>{children}</div>;
}

function AlbumSlotView({ slot, photoUrls }: { slot: AlbumSlot; photoUrls: Map<string, string> }) {
  const card = slot.copies[0];
  if (!card) return <SlotFrame index={slot.index}><Link aria-label={`${slot.player.display_name} — not collected`} className="relative block aspect-[5/7] rounded-[0.9rem] border border-dashed border-brass/40 bg-board-deep/70 p-3 shadow-inner outline-offset-4 outline-brass focus-visible:outline-2" href={`/players/${slot.player.slug}`}><svg aria-hidden="true" className="h-3/5 w-full stroke-brass/20" fill="none" viewBox="0 0 200 200"><path d="M64,28 L84,32 Q100,48 116,32 L136,28 L164,44 L180,92 L150,108 L144,94 L144,180 L56,180 L56,94 L50,108 L20,92 L36,44 Z" strokeWidth="4" /></svg><span className="absolute bottom-3 left-3 right-3 border-t border-brass/20 pt-2 text-xs font-bold text-ink-faint">{slot.player.display_name}</span><span className="tier-chip absolute right-2 top-8 h-5 w-5" data-rarity={slot.player.rarity_tier}><span /></span></Link></SlotFrame>;
  const player: LiveCardPlayer = { id: card.card_id, displayName: card.display_name, archetype: card.archetype, liveOvr: card.live_ovr, pac: card.pac, sho: card.sho, pas: card.pas, dri: card.dri, def: card.def, phy: card.phy, rarityTier: card.rarity_tier, photoUrl: card.photo_path ? photoUrls.get(card.photo_path) ?? null : null };
  return <SlotFrame index={slot.index}>{slot.copies.length > 1 && <span aria-hidden="true" className="absolute inset-x-1 top-5 z-0 h-[96%] rounded-[0.9rem] border border-line bg-panel-2 translate-x-1.5 translate-y-1.5" />}<Link aria-label={`${slot.player.display_name} — collected`} className="relative z-10 block rounded-[0.9rem] outline-offset-4 outline-brass focus-visible:outline-2" href={`/club/collection/${card.card_id}`}><LiveCard player={player} /></Link>{card.active_listing_id && <span className="absolute left-1/2 top-8 z-20 -translate-x-1/2 rounded-full border border-brass/55 bg-board-deep/90 px-2 py-1 text-[0.55rem] font-black uppercase tracking-[0.1em] text-brass">Listed</span>}{card.held_by_offer_id && <span className="absolute left-1/2 top-8 z-20 -translate-x-1/2 whitespace-nowrap rounded-full border border-brass/55 bg-board-deep/90 px-2 py-1 text-[0.55rem] font-black uppercase tracking-[0.1em] text-brass">In a trade offer</span>}{slot.copies.length > 1 && <Link className="absolute bottom-1 right-1 z-20 rounded-full bg-brass px-2 py-0.5 text-xs font-black text-ink-on-accent" href={`/club/collection?view=manage&q=${encodeURIComponent(slot.player.display_name)}`}>×{slot.copies.length}</Link>}</SlotFrame>;
}

function Leaf({ number, slots, photoUrls }: { number: number; slots: AlbumSlot[]; photoUrls: Map<string, string> }) { const collected = slots.filter((slot) => slot.copies.length > 0).length; return <article className="relative min-w-0 rounded-3xl border border-line/60 bg-panel/75 p-5 shadow-2xl"><header className="mb-4 flex items-baseline justify-between border-b border-line/50 pb-4"><div><h2 className="display text-3xl">Page {number}</h2><p className="mt-1 text-[0.6rem] font-extrabold uppercase tracking-[0.14em] text-ink-faint">{slots[0] ? `slots ${slots[0].index}–${slots[slots.length - 1].index}` : "No slots"}</p></div><span className="text-sm font-black tabular-nums text-brass">{collected} / {slots.length}</span></header><div className="grid grid-cols-2 gap-4 sm:grid-cols-3">{slots.map((slot) => <AlbumSlotView key={slot.player.id} photoUrls={photoUrls} slot={slot} />)}</div><span className="mt-4 block text-right text-xs font-bold tabular-nums text-ink-faint">{number}</span></article>; }

export function CollectionAlbum({ roster, cards, lensValue, pageValue, ownPlayerId, photoUrls, clubValue }: Props) {
  const allSlots = buildSlots(roster, cards); const lens = parseLens(lensValue); const selectedLens = lensParam(lens); const filtered = applyLens(allSlots, lens); const requested = pageValue === "all" ? "all" : Number(pageValue ?? 1); const { pages, total } = paginate(filtered, typeof requested === "number" ? requested : 1); const currentPage = typeof requested === "number" ? requested : 1; const [left, right] = spreadFor(currentPage); const spreadPages = [[left, pages[left - 1] ?? []], right !== null && right <= total ? [right, pages[right - 1] ?? []] : null].filter(Boolean) as [number, AlbumSlot[]][]; const unique = allSlots.filter((slot) => slot.copies.length > 0).length; const own = ownPlayerId ? roster.find((player) => player.id === ownPlayerId) : null; const previousPage = currentPage > 1 ? currentPage - 1 : undefined; const nextPage = currentPage < total ? currentPage + 1 : undefined; const previousSpread = left > 1 ? left - 2 : undefined; const nextSpread = right !== null && right < total ? right + 1 : undefined;
  // Desktop opens a spread, so two leaves are on screen and both chips must read
  // as open (KB-004). The partner is highlighted at `lg:` only, because the second
  // leaf is revealed purely by CSS (`hidden lg:block`) and BUILD_SPEC §41 requires
  // identical page numbers at every width — so we mark it, never renumber it.
  // In the all-pages view every leaf is already on screen, so there is no partner.
  const partnerPage = requested === "all" ? null : spreadPartner(currentPage, total);
  const typeItems = ARCHETYPES.map((archetype) => ({ href: href(`type:${archetype}`, 1), label: ARCHETYPE_LABELS[archetype] })); const tierItems = TIERS.map((tier) => ({ href: href(`tier:${tier}`, 1), label: tier[0].toUpperCase() + tier.slice(1) }));
  return <div className="space-y-8"><CollectionHeader cardCount={cards.length} clubValue={clubValue} discardValue={cards.reduce((sum, card) => sum + Number(card.discard_value), 0)} mode="album" showArchetypeNudge={own?.archetype === "all_rounder"} totalPlayers={allSlots.length} uniquePlayers={unique} /><nav aria-label="Album lens" className="flex flex-wrap items-center gap-2 border-y border-line/40 py-4"><span className="mr-2 text-[0.65rem] font-extrabold uppercase tracking-[0.15em] text-ink-faint">Lens</span>{(["all", "gaps", "specialists"] as const).map((value) => <Link className={`min-h-10 rounded-full border px-4 py-2 text-xs font-extrabold ${selectedLens === value ? "border-brass/60 bg-brass/12 text-brass" : "border-line text-ink-dim"}`} href={href(value, 1)} key={value}>{value === "all" ? `All players ${allSlots.length}` : value === "gaps" ? `Gaps only ${allSlots.length - unique}` : `Specialists ${allSlots.filter((slot) => slot.player.archetype !== "all_rounder").length}`}</Link>)}<LensMenu items={typeItems} label="By type" /><LensMenu items={tierItems} label="By tier" /></nav>{requested === "all" ? <div className="space-y-6">{pages.map((slots, index) => <Leaf key={index} number={index + 1} photoUrls={photoUrls} slots={slots} />)}</div> : <AlbumKeyboardNavigation nextHref={nextPage ? href(selectedLens, nextPage) : undefined} previousHref={previousPage ? href(selectedLens, previousPage) : undefined}><div className="grid gap-3 lg:grid-cols-[3rem_minmax(0,1fr)_3rem] lg:items-center"><div className="hidden lg:block">{previousSpread && <Link aria-label={`Previous pages, pages ${previousSpread}–${previousSpread + 1}`} className="grid h-12 w-12 place-items-center rounded-full border border-line text-2xl text-ink-dim hover:border-brass hover:text-brass" href={href(selectedLens, previousSpread)}>‹</Link>}</div><div className="grid gap-0 lg:grid-cols-2">{spreadPages.map(([number, slots]) => <div className={number === currentPage ? "" : "hidden lg:block"} key={number}><Leaf number={number} photoUrls={photoUrls} slots={slots} /></div>)}</div><div className="hidden justify-end lg:flex">{nextSpread && <Link aria-label={`Next pages, pages ${nextSpread}${nextSpread + 1 <= total ? `–${nextSpread + 1}` : ""}`} className="grid h-12 w-12 place-items-center rounded-full border border-brass/60 text-2xl text-brass hover:bg-brass/10" href={href(selectedLens, nextSpread)}>›</Link>}</div></div><div className="mt-3 grid grid-cols-2 gap-3 lg:hidden">{previousPage ? <Link className="min-h-11 rounded-xl border border-line px-4 py-3 text-center font-bold" href={href(selectedLens, previousPage)}>‹ Page {previousPage}</Link> : <span />}{nextPage ? <Link className="min-h-11 rounded-xl border border-brass/60 px-4 py-3 text-center font-bold text-brass" href={href(selectedLens, nextPage)}>Page {nextPage} ›</Link> : <span />}</div></AlbumKeyboardNavigation>}<nav aria-label="Album pages" className="flex flex-wrap justify-center gap-2">{pages.map((page, index) => { const number = index + 1; const count = page.filter((slot) => slot.copies.length > 0).length; return <Link aria-current={number === currentPage ? "page" : undefined} className={`w-14 rounded-lg border p-2 text-center text-xs font-black ${number === currentPage ? "border-brass text-brass" : number === partnerPage ? "border-line text-ink-dim lg:border-brass lg:text-brass" : "border-line text-ink-dim"}`} href={href(selectedLens, number)} key={number}>{number}<Completion count={count} total={page.length} /></Link>; })}</nav><Link className="block text-center text-sm font-bold text-brass hover:underline" href={href(selectedLens, "all")}>Show all pages</Link></div>;
}
